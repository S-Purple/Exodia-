# Exodia

A simple Vite + React app showing five body-part cards in a two-row layout.

## Setup

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — start dev server
- `npm run build` — production build
- `npm run preview` — preview production build
